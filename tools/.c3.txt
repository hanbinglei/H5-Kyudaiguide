README: 更新到当前状态

原文有三处已经不准确了：
  · 「12 scenarios」→ 实际是 17 篇正文
  · 「no backend」→ 反馈链路确实有后端（Apps Script），但**只用于反馈**，
    阅读本身仍是无服务端的静态站 —— 这点要说清楚，否则误导
  · 缺状态条 / 出处小节 / 反馈闭环 / 离线安装 / 四语内容完整度 / 搜索架构

新增「Quality gates」一节：把 14 个校验器列出来。这是本项目真正的资产之一 ——
「改完必须跑检查」不是靠自觉，而是有工具强制，且工具本身也进版本控制。

Limitations 改为诚实版：
  · 搜索精度四语不均（日文某些查询仍返回 9〜16 / 17 篇）
  · 英文词表缺口（查询说 sick，正文写 unwell）
  · 只在 Chrome 验证过，iOS Safari 未测 —— 而读者主力就是 iPhone
  · 除作者外没有真实用户：上面所有检查都是自测，真实用户才会遇到的
    「这句话看不懂」「这段没人读完」这类问题，检查器结构性地看不见
